name: Build (reusable)

# Single source of truth for the tds-tools site pipeline. Called by ci.yml
# (PR gate, no publish), dev.yml (demo build → dev branch) and release.yml
# (production build → release branch + deploy ping). Behaviour selected through
# the inputs. See AGENTS.md for the --no-package-lock / NPM_TOKEN rationale.

on:
  workflow_call:
    inputs:
      demo_mode:
        description: Value for PUBLIC_DEMO_MODE on publishing builds (dev=true, release=false).
        type: boolean
        default: false
      publish_branch:
        description: Orphan branch to force-publish dist/ to (dev | release). Empty = no publish.
        type: string
        default: ''
      deploy:
        description: Ping the deploy webhook after publishing (release only).
        type: boolean
        default: false

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5

      - name: Set up Node
        uses: actions/setup-node@v5
        with:
          node-version: '22'
          registry-url: 'https://npm.pkg.github.com'
          scope: '@tracht-digital-solutions'

      - name: Pin npm
        # Node 22 ships npm 10, whose arborist crashes with
        # "Cannot read properties of null (reading 'edgesOut')" on some
        # dependency graphs. Astro 7.3.0 (2026-09-03) introduced one: the first
        # build after that publish failed at Install, before a single test ran,
        # on a commit that touched no manifest. npm 11 resolves the same tree
        # without complaint.
        #
        # This bites here and not elsewhere because `--no-package-lock` below
        # re-resolves every caret range on every run — so a publish by any
        # dependency can change the graph between two builds of the same
        # commit. That is the trade for a lockfile this repo cannot use; the
        # cost is that the npm doing the resolving has to be current.
        # tds-landingpage-frontend hit this first and carries the same step.
        run: npm install -g npm@11

      - name: Install
        # Windows-generated lockfile is win32-only; --no-package-lock resolves
        # the right native binaries on the Linux runner (npm/cli#4828).
        run: npm install --no-package-lock
        env:
          NPM_TOKEN: ${{ secrets.PACKAGE_TOKEN }}

      - name: Type check
        run: npm run type-check

      - name: Lint primitives
        # This site renders on the `blog` surface (moved there 2026-08-17), so
        # its geometry has to come from that layer's tokens. Hand-authored radii
        # bypass them: the gate box and the tool box were both `rounded-2xl`
        # (16px) while the card token is 8px. Scoped to src/ — dist/ is output.
        run: npm run lint:primitives

      - name: Test
        # 171 tests that only ever ran on a developer machine. Four of them are
        # the design freeze (surface/header/marketing/seo); a suite that gates
        # nothing cannot hold a freeze.
        run: npm run test:run

      - name: Configure demo mode
        if: ${{ inputs.publish_branch != '' }}
        run: echo "PUBLIC_DEMO_MODE=${{ inputs.demo_mode }}" >> "$GITHUB_ENV"

      - name: Build
        run: npm run build

      - name: Stamp build
        if: ${{ inputs.publish_branch != '' }}
        run: |
          mkdir -p release/client
          {
            # The only place the deployed version is observable. The site renders
            # it nowhere — no footer line, no meta tag — so without this a
            # release cannot be told apart from the one before it.
            echo "version=$(node -p "require('./package.json').version")"
            echo "channel=${{ inputs.publish_branch }}"
            echo "commit=${GITHUB_SHA}"
            echo "built_at=$(date -u +%Y-%m-%dT%H:%M:%SZ)"
            echo "workflow_run=${GITHUB_RUN_ID}"
          } > release/client/.build-info

      - name: Publish release tree to ${{ inputs.publish_branch }} branch
        if: ${{ inputs.publish_branch != '' }}
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.PACKAGE_TOKEN }}
          # `release/`, not `dist/`: this site is server-rendered now, so the
          # host needs the SSR bundle, the Passenger startup file and a
          # prebuilt node_modules alongside the document root. The tree is
          # assembled by scripts/pack-release.mjs, which runs as postbuild and
          # refuses to produce one that could not start on the host.
          publish_dir: ./release
          publish_branch: ${{ inputs.publish_branch }}
          commit_message: '${{ inputs.publish_branch }} ${{ github.sha }}'
          force_orphan: true

      - name: Trigger deploy webhook
        if: ${{ inputs.deploy }}
        env:
          DEPLOY_WEBHOOK_URL: ${{ secrets.DEPLOY_WEBHOOK_URL }}
        run: |
          if [ -z "$DEPLOY_WEBHOOK_URL" ]; then
            echo "DEPLOY_WEBHOOK_URL not set - skipping deploy ping."
            exit 0
          fi
          echo "POSTing deploy webhook to $(printf '%s' "$DEPLOY_WEBHOOK_URL" | cut -d/ -f1-3) ..."
          code=$(curl -sS -o /dev/null -w '%{http_code}' \
            -X POST -L --post301 --post302 --post303 \
            --retry 3 --retry-delay 5 --max-time 30 \
            "$DEPLOY_WEBHOOK_URL" || echo "000")
          if [ "$code" -ge 200 ] && [ "$code" -lt 300 ]; then
            echo "Deploy webhook pinged (HTTP $code)."
          else
            echo "::warning title=Deploy webhook failed::Webhook returned HTTP $code. The release branch is published but the host was not notified - check DEPLOY_WEBHOOK_URL."
          fi

      # The host clears its own page cache; this only warms it again.
      #
      # `resolveCacheDirs()` in tds-shared runs at every boot — and a deploy
      # restarts the process — where `discardCacheOfOtherBuild()` fingerprints
      # the sorted filenames in `client/_astro`. Those are content-hashed, so a
      # new build always yields a new fingerprint; on a mismatch it empties both
      # the page and the meta directory. The purge therefore needs no endpoint,
      # no token and no step here, and it is strictly more reliable than one:
      # it cannot race the deploy, cannot be skipped, and covers a deploy made
      # by hand or a rollback just the same.
      #
      # What is left for CI is the half the library does NOT do: filling the
      # cache again, so the first real visitor does not pay a render. That is
      # plain public GETs — no secret of any kind.
      - name: Warm the page cache
        if: ${{ inputs.deploy }}
        run: |
          # Never `set -e`: release/ is published and the host is already
          # notified by this point, so nothing below may red the run. Every
          # failure is a warning and an early, successful exit.
          set +e

          # The origin comes from the repo, because it is not a secret — it is
          # the canonical address this site publishes under. `astro.config.mjs`
          # is where Astro itself keeps it (`site:`), so a domain change moves
          # this along with everything else instead of leaving a copy behind in
          # repository settings that nobody remembers to update.
          origin=$(grep -o 'site: *"https://[^"]*"' astro.config.mjs | head -1 | sed 's/.*"\(https:\/\/[^"]*\)".*/\1/')
          origin="${origin%/}"
          if [ -z "$origin" ]; then
            echo "::warning title=Cache not warmed::No 'site' found in astro.config.mjs, so there is no origin to warm. The cache is empty and correct; visitors will refill it."
            exit 0
          fi

          # ---- Wait until the host serves THIS commit ----------------------
          # The deploy webhook is fire-and-forget and the host pulls
          # asynchronously. Warming before the new build is live would fill the
          # cache with the OLD build's pages — which the restart would then
          # discard anyway, so the work would simply be thrown away.
          deadline=$(( $(date +%s) + 300 ))
          live=""
          while [ "$(date +%s)" -lt "$deadline" ]; do
            info=$(curl -sS -L --max-time 15 "${origin}/.build-info" 2>/dev/null)
            case "$info" in
              *"commit=${GITHUB_SHA}"*) live="yes"; break ;;
            esac
            sleep 10
          done
          if [ -z "$live" ]; then
            echo "::warning title=Cache not warmed::${origin} is still not serving ${GITHUB_SHA} after 5 minutes. Nothing was warmed - the cache is empty and correct, and visitors will refill it."
            exit 0
          fi
          echo "Host is serving ${GITHUB_SHA}; its cache was discarded on boot."


          # The site's own sitemap is the route list, so this warms exactly what
          # is published and needs no second list to keep in step.
          urls=$(curl -sS -L --max-time 30 "${origin}/sitemap-0.xml" 2>/dev/null \
            | grep -o '<loc>[^<]*</loc>' | sed 's|</\?loc>||g' | head -40)
          if [ -z "$urls" ]; then
            echo "::warning title=Cache left cold::Could not read ${origin}/sitemap-0.xml, so no page was warmed. The cache is empty and correct; visitors will refill it."
            exit 0
          fi

          # The FIRST page is the smoke test, and it replaces a gate that asked
          # the wrong question.
          #
          # This used to check /tds/connect/status for a "connected" site. Two
          # real release runs showed production reports neither `connected` nor
          # `legacy_environment` — that endpoint describes CMS PAIRING, which
          # these sites do not rely on, so the gate skipped the warm-up every
          # time while the site served traffic perfectly.
          #
          # Rendering one page and looking at the status code says something
          # true instead: if the very first request does not come back 2xx, the
          # deploy is broken in a way worth stopping for, and warming 40 pages
          # would only cache 40 copies of the problem. It does NOT claim to
          # detect a degraded upstream — the content fetches are fail-soft, so
          # a page returns 200 with a section missing. That risk is the known,
          # accepted cost of warming at all.
          warmed=0
          failed=0
          first=1
          for u in $urls; do
            c=$(curl -sS -o /dev/null -w '%{http_code}' -L --max-time 45 "$u" 2>/dev/null)
            if [ "${c:-000}" -ge 200 ] && [ "${c:-000}" -lt 300 ]; then
              warmed=$((warmed + 1))
            else
              failed=$((failed + 1))
              echo "  warm ${u} -> HTTP ${c:-000}"
              if [ "$first" = "1" ]; then
                echo "::warning title=Cache left cold::The first page (${u}) answered HTTP ${c:-000}, so the warm-up stopped there rather than caching the same failure across the site. The cache is empty and correct; visitors will refill it."
                exit 0
              fi
            fi
            first=0
            # A moment between renders: this is a single-process Node host and
            # the point is a warm cache, not a load test against our own site.
            sleep 1
          done
          echo "Page cache warmed: ${warmed} ok, ${failed} failed."
          if [ "$failed" -gt 0 ]; then
            echo "::warning title=Some pages did not warm::${failed} of $((warmed + failed)) sitemap URLs did not return 2xx. They are simply uncached - the first visitor renders them."
          fi
